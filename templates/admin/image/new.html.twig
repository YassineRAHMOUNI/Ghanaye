{% extends 'admin/base.html.twig' %}

{% block title %}New Image{% endblock %}
{% block content %}
    <h1>Add New Image to Gallery</h1>

    {{ include('admin/image/_form.html.twig') }}

<table class="table">
    <tbody>
    <tr>
        <th>Picture</th>
        <th>Delete</th>
    </tr>
    {% for image in imagelist %}
    <tr>
        <td> <img src="{{ asset('uploads') }}/images/{{ image.image }}" height="150"> </td>

       <td><a href="{{ path('admin_image_mydelete',{'id':image.id,'pid':image.productid}) }}" onclick="return confirm('Are You Sure to Delete?')" class="btn btn-danger btn-xs"><span class="glyphicon glyphicon-remove"></span>&nbsp;&nbsp; Delete</a> </td>
    </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
